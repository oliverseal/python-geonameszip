A quick and dirty library for syncing postal codes / zip codes with a local database.

Main uses are for postal code validation:

```python
import geonameszip
geonameszip.lookup_postal_code('77098')
# returns {}...
```
